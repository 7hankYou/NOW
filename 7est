```html
<!DOCTYPE html>
<html>
<head>
    <script src="https://d3js.org/d3.v4.min.js"></script>
    <style>
        body { 
            margin: 0; 
            padding: 0; 
            background: #000; 
        }
        .sacred-circle {
            fill: none;
            stroke: rgba(0,255,0,0.6);
            stroke-width: 2;
            animation: pulse 3s infinite;
        }
        @keyframes pulse {
            0% { opacity: 0.8; stroke: rgba(0,255,0,0.6); }
            50% { opacity: 0.4; stroke: rgba(0,0,255,0.6); }
            100% { opacity: 0.8; stroke: rgba(0,255,0,0.6); }
        }
    </style>
</head>
<body>
    <script>
        const width = 800;
        const height = 800;
        
        const svg = d3.select("body")
            .append("svg")
            .attr("width", width)
            .attr("height", height)
            .style("background-color", "#000");

        function createFlowerOfLife() {
            const radius = 100;
            const centerX = width/2;
            const centerY = height/2;

            // Center circle
            svg.append("circle")
                .attr("class", "sacred-circle")
                .attr("cx", centerX)
                .attr("cy", centerY)
                .attr("r", radius);

            // Surrounding circles
            for (let i = 0; i < 6; i++) {
                const angle = (i * Math.PI * 2) / 6;
                const x = centerX + radius * Math.cos(angle);
                const y = centerY + radius * Math.sin(angle);
                
                svg.append("circle")
                    .attr("class", "sacred-circle")
                    .attr("cx", x)
                    .attr("cy", y)
                    .attr("r", radius);
            }
        }

        createFlowerOfLife();
    </script>
</body>
</html>
```
